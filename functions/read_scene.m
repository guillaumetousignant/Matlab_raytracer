function read_scene(filename, varargin)
    scene_struct = xml2struct(filename);

    % Colours
    load colours_mat colours

    %[num, status] = str2num(str) % will work for matrices, NaN, inf, i etc.
end